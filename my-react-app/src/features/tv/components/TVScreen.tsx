import { Box } from '@mui/material';

export default function TVScreen() {
  return (
    <Box
      sx={{
        width: '100%',
        height: '100%',
        backgroundColor: 'black',
      }}
    />
  );
}
